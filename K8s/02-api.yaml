apiVersion: v1
kind: Secret
metadata:
  name: api-secret
type: Opaque
stringData:
  connection-string: "Server=app-mysql;Port=3306;Database=footballdb;User=root;Password=rootpassword;"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: football-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: football-api
  template:
    metadata:
      labels:
        app: football-api
    spec:
      containers:
        - name: football-api
          image: <ACR_NAME>.azurecr.io/football-api:latest
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: api-secret
                  key: connection-string
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: football-api
spec:
  selector:
    app: football-api
  ports:
    - port: 5000
      targetPort: 8080
